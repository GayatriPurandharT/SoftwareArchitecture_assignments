<p id="notice"><%= notice %></p>

<h1>ReadOn</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @themains.each do |themain| %>
      <tr>
        <td><%= link_to 'Show', themain %></td>
        <td><%= link_to 'Edit', edit_themain_path(themain) %></td>
        <td><%= link_to 'Destroy', themain, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Themain', new_themain_path %>
